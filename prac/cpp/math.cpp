//Base math

int main() {
    int a = {}
    double x = 10;
    int y = 3;
    double z = x + y;
    std:cout << z; a;
    return 0
}

//double = floating point
//int = integer

// % = modulus = returns the remainder
// x++; = Increment operator; x--; = Decrement operator
// y = ++x; = Increments first, then stores in y

// **Data Types**
// short = numbers below 32000, smaller data
// int = basic numbers. -2million - 2 million
// long = ((Value must be entered as xyzL to prevent data loss))
// long long

// float = lower memory, -3.4e38 to 3.4e38 ((Value must be entered as x.yzF to prevent data loss))
// double = -1.7e308 to 1.7e308
// long double = -3.4e932 to 1.74e4832

// bool = true / false
// char = single character

// auto = Will automatically match data type to the value its storing