#include <iostream>  // Preprocessor directive that includes the standard input/output stream

int main() { // This initiates the MAIN function which is always named that.
    std::cout << "Hello, World!" << std::endl;  // Output "Hello, World!" to the console
    return 0;  // There is no "check" in this mini program, so the function will not know to quit without this line.
}

